/* details 服务 */
angular.module('starter.details.service', [])
    .factory('detailsService', [
        '$http',
        '$q',
        function ($http, $q) {
            return {

            }
        }]);
